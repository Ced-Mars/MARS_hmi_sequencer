import React, { useEffect, useState } from "react";
import Button from '@mui/material/Button';

import socketIOClient from "socket.io-client";

export default function Dialogue(){


    return(
        <div>
            <p>Zone de comm avec user</p>
        </div>
    );
}